class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        diffrences = {}
        for i in range(len(nums)): 
            if nums[i] in diffrences:
                return [i, diffrences[nums[i]]]
            diffrences[target - nums[i]] = i
